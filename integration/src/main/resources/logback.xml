<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- TODO: move to cisco-spark-base -->
    <contextName>${environment:-localtest}</contextName>

    <include optional="true" resource="includedLogbackConfig.xml"/>
    <include optional="true" resource="logback-included.xml"/>

    <property name="CONSOLE_LOG_PATTERN"
              value="${CONSOLE_LOG_PATTERN:-%clr(%d{HH:mm:ss.SSS}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} ${EXTRA_CONSOLE_LOG_PATTERN:- } %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}}"/>

    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <!-- Ensure that logback level changes are also propagated to java.util.logging framework -->
    <!-- See http://logback.qos.ch/manual/configuration.html#LevelChangePropagator -->
    <!-- NOTE: Oddly, this needs to be at the end of the configuration
            See: http://jira.qos.ch/browse/LOGBACK-740
                 http://stackoverflow.com/a/11798287/259466 -->
    <contextListener class="ch.qos.logback.classic.jul.LevelChangePropagator"/>
</configuration>
